# Devenv
.devenv*
devenv.local.nix
devenv.local.yaml

# direnv
.direnv

# pre-commit
.pre-commit-config.yaml

# Pi local state (keep the shared MCP configuration)
.pi/*
!.pi/mcp.json

# env files
.env
.env.local
